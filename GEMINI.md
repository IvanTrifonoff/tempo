# ⚡️ Gemini Context: Tempo Project (v2.1)

## 🛑 АВАРИЙНЫЙ ПРОТОКОЛ (READ FIRST)
Предыдущий агент совершил ошибки: спешил, не читал документацию.
**Твоя главная задача: Не ломать ПРОД (3005).**

### 1. Зона Ответственности
Твоя рабочая директория: `tempo/extracted/tempo-main`.

### 2. Инфраструктура (Hard Truth)
*   **Сервер:** `82.202.141.81` (user: `admssh`).
*   **БД:** PostgreSQL. Схема управляется через `npm run migrate`.
*   **PROD:** Порт **3005**. Деплой только после тестов.
*   **TEST:** Порт **3006**. Песочница.

### 3. Структура Бэкенда (Modular)
Не пиши все в `server.js`!
*   **Роуты:** `server/routes/`
*   **Контроллеры:** `server/controllers/` (используй `asyncHandler`)
*   **БД:** `server/db/index.js`

### 4. Алгоритм Действий
1.  **Исследование:** Прочитай `TECH_DEBT.md`.
2.  **Разработка:**
    *   Если меняешь БД -> Создай миграцию (`node-pg-migrate create name`).
    *   Если меняешь код -> Деплой на TEST.
3.  **Сдача:** Проверь логи `docker logs tempo-test-pg`.

### 5. CI/CD
Настроен GitHub Actions (`.github/workflows/deploy.yml`).
Для активации нужно добавить секреты (`SSH_HOST`, `SSH_KEY`, etc) в репозиторий.
Пока секретов нет — деплоим вручную архивом.