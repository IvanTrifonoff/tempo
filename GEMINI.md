# ⚡️ Gemini Context: Tempo Project (v2.0)

## 🛑 АВАРИЙНЫЙ ПРОТОКОЛ (READ FIRST)
Предыдущий агент совершил ошибки: спешил, не читал документацию, пытался деплоить на ПРОД без тестов. Не повторяй этого.

### 1. Зона Ответственности
Твоя рабочая директория: `tempo/extracted/tempo-main`.
Все, что выше — не трогай без крайней необходимости.

### 2. Инфраструктура (Hard Truth)
*   **Сервер:** `82.202.141.81` (user: `admssh`).
*   **БД:** PostgreSQL (в контейнере `mautrix-telegram-db-1`, база `tempo`). **JSON-файлы мертвы.**
*   **PROD:** Порт **3005** (`tempo-app`). **СВЯЩЕННАЯ КОРОВА. НЕ ТРОГАТЬ БЕЗ ПРИКАЗА.**
*   **TEST:** Порт **3006** (`tempo-test-pg`). Здесь ты живешь, ломаешь и чинишь.

### 3. Алгоритм Действий (The Pipeline)
1.  **Исследование:**
    *   Прочитай `tempo/extracted/tempo-main/AGENT_CONTEXT.md` (Главный файл).
    *   Прочитай `tempo/extracted/tempo-main/DESIGN_SYSTEM.md` (Если трогаешь UI).
2.  **Разработка:**
    *   Вноси правки.
    *   Деплой на **TEST** (3006).
    *   Запуск тестов: `sudo docker exec -i tempo-test-pg npm test`.
3.  **Сдача работы:**
    *   Покажи результат в Тесте.
    *   Только после "ОК" от пользователя — деплой на ПРОД.

### 4. Технические Нюансы
*   **Frontend:** `App.tsx` разбит на хуки (`hooks/`). Не пихай все в один файл.
*   **UI:** Используй `components/EditTrackModal.tsx` как пример стиля.
*   **Audio:** Помни про iOS Safari (`useAudioContext.ts`).

Следуй этому, и ты выживешь.
